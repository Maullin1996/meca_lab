name: meca_lab
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  atomic_design:
    git:
      url: https://github.com/Maullin1996/atomic_design.git
  fpdart: ^1.2.0
  shared_preferences: ^2.5.5
  flutter_riverpod: ^3.3.2
  riverpod_annotation: ^4.0.3
  go_router: ^17.3.0
  fl_chart: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.5
  riverpod_generator: ^4.0.4
  build_runner: ^2.15.1
  fake_async: ^1.3.3

flutter:
  uses-material-design: true
  assets:
    - assets/config/app_config.json
